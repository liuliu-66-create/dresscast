# 虚拟试穿带货视频生成器 — 安装与使用指南

> 本指南面向零基础小白用户。
>
> 在使用 Claude Code 搭建时，我以 GLM-5.1 模型为例。请注意，Claude Code 支持多种模型，您可以根据需要选择其他模型进行替换。

---

## 你能用它做什么？

给 AI 两张图片（一张衣服 + 一张模特），它会自动：
1. 把衣服"穿"到模特身上，生成正面、侧面、背面、特写 4 张效果图
2. 用正面效果图生成一段 10 秒的带语音带货营销视频
3. 把所有结果上传到你的飞书多维表格

**整个过程全自动，你只需要提供两张图片。**

---

## 安装流程（大部分由 AI 自动完成）

> 以下所有步骤，**除了获取 API 密钥需要你自己操作外，其余都可以直接让 AI 帮你完成**。遇到任何问题，随时问 AI 即可。

### 1. 安装技能

在 Claude Code 对话框中，直接说：

```
请帮我安装这个技能：https://github.com/liuliu-66-create/tryon-video-generator
```

AI 会自动把技能下载到本地。

### 2. 安装运行环境

AI 会帮你检查并安装所需的 Python 环境、依赖库等。如果缺少什么，直接告诉 AI：

```
请帮我配置试穿视频技能的运行环境
```

AI 会自动安装所有需要的东西（Python、pip 依赖等），你只需要在提示时点确认即可。

### 3. 获取阿里云百炼 API Key（需要你手动操作）

这一步**必须你亲自完成**，因为涉及账号注册和实名认证。

1. 打开浏览器，访问 https://bailian.console.aliyun.com/
2. 用支付宝或淘宝账号登录
3. 按提示完成实名认证（如果还没认证过）
4. 登录后，点击左侧菜单的 **「API-KEY 管理」**
5. 点击 **「创建 API Key」**
6. 复制生成的 Key（格式类似 `sk-xxxxxxxxxxxx`），**保存好，只显示一次**

拿到 Key 后，告诉 AI：

```
请帮我配置试穿视频技能的 API Key，我的 Key 是：sk-xxxxxxxxxxxx
```

AI 会自动帮你写入配置文件。

### 4. 飞书上传（可选）

如果你想把结果自动上传到飞书多维表格，告诉 AI：

```
请帮我配置飞书上传功能
```

AI 会帮你安装所需的工具并完成登录授权。如果不需要飞书上传，跳过这步即可，效果图和视频会保存在本地。

---

## 开始使用

一切就绪！直接对 AI 说以下任意一句即可触发：

- `帮我生成试穿视频`
- `试穿视频`
- `虚拟试穿`
- `生成带货视频`

### 你需要提供什么

AI 会问你要两张图片：
- **产品图**：一张服装照片（上衣、裙子、裤子等，白底或平铺效果最好）
- **模特图**：一张人物全身或半身照

把图片的文件路径发给 AI，比如：
```
产品图：C:/Users/我的名字/Desktop/dress.jpg
模特图：C:/Users/我的名字/Desktop/model.jpg
```

> 小技巧：可以直接把图片文件拖进 VS Code 终端，会自动显示路径。

### 然后就等着

AI 会全自动完成所有步骤（约 5-15 分钟），中间不需要你做任何操作。

### 查看结果

AI 最后会告诉你：
- 4 张效果图的保存位置（正面/侧面/背面/特写）
- 视频文件的保存位置
- 飞书表格的链接（如果配置了飞书上传）

所有文件都在 `~/.claude/skills/tryon-video-generator/workspace/` 目录下。

---

## 常见问题

### Q：提示「未找到 DASHSCOPE_API_KEY」
**A**：API Key 没配置好。直接告诉 AI：「请帮我检查试穿视频技能的 API Key 配置」，它会帮你排查。

### Q：图片分析失败
**A**：检查图片格式是否为 JPEG 或 PNG，文件大小是否小于 20MB。微信传图可能被压缩，建议用原图。

### Q：融合效果不好，衣服没有完整穿上去
**A**：这是正常的，AI 有时不完美。系统会自动验证正面图并重试一次。如果还是不满意，可以换一张更清晰的产品图（白底、平铺效果最好）。

### Q：视频生成等了很久
**A**：视频生成通常需要 3-10 分钟，请耐心等待。超过 10 分钟会超时，可以再试一次。

### Q：没有飞书表格链接
**A**：需要配置飞书上传功能（安装流程第 4 步）。如果不需要飞书上传，可以直接去 workspace 目录查看本地文件。

### Q：config.example.json 和 config.json 有什么区别
**A**：`config.example.json` 是配置模板（不含真实密钥），`config.json` 是你的个人配置（含 API Key）。技能运行时只读取 `config.json`。

---

## 技术参数一览

| 项目 | 说明 |
|------|------|
| 图片分析模型 | qwen3-vl-flash |
| 虚拟试穿模型 | wan2.7-image |
| 视频生成模型 | wan2.6-i2v-flash |
| 视频时长 | 固定 10 秒 |
| 视频分辨率 | 720P |
| 视频音频 | 有声（自动生成语音） |
