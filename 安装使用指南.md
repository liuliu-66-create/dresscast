# 虚拟试穿带货视频生成器 — 安装与使用指南

> 本指南面向零基础小白用户，每一步都有详细说明，跟着做就行。

---

## 你能用它做什么？

给 Claude 两张图片（一张衣服 + 一张模特），它会自动：
1. 把衣服"穿"到模特身上，生成正面、侧面、背面、特写 4 张效果图
2. 用正面效果图生成一段 10 秒的带语音带货营销视频
3. 把所有结果上传到你的飞书多维表格

**整个过程全自动，你只需要提供两张图片。**

---

## 第一步：安装 Python（如果还没装）

> Python 是运行这个技能必需的程序。如果不确定有没有装，先试第二步——如果报错就回来装。

1. 打开浏览器，访问 https://www.python.org/downloads/
2. 点击黄色的 **"Download Python 3.x.x"** 按钮
3. 双击下载的安装程序
4. **重要**：安装界面底部勾选 ✅ **"Add python.exe to PATH"**
5. 点击 **"Install Now"**
6. 等待安装完成，点 Close

**验证安装**：打开终端（VS Code 里按 `` Ctrl + ` ``），输入：
```
python --version
```
看到类似 `Python 3.12.x` 就说明装好了。

---

## 第二步：安装技能

在 Claude Code 对话框中，直接对 Claude 说：

```
请帮我安装这个技能：https://github.com/liuliu-66-create/tryon-video-generator
```

Claude 会自动帮你把技能克隆到本地。如果 Claude 问你要不要确认，点确认就行。

安装完成后你会看到技能目录出现在你的 skills 文件夹中。

---

## 第三步：安装 Python 依赖

这个技能需要一个叫 `requests` 的 Python 库。在终端中运行：

```
pip install requests
```

看到 `Successfully installed requests` 就说明装好了。

---

## 第四步：获取阿里云百炼 API Key

这是使用 AI 模型必需的密钥，**需要实名认证**，但**注册免费**，模型按次计费（完成一次试穿+视频约 3.8 元）。

1. 打开浏览器，访问 https://bailian.console.aliyun.com/
2. 用你的支付宝或淘宝账号登录
3. 如果是第一次使用，按提示完成实名认证
4. 登录后，点击左侧菜单的 **「API-KEY 管理」**
5. 点击 **「创建 API Key」**
6. 复制生成的 Key（格式类似 `sk-xxxxxxxxxxxx`），**保存好，只显示一次**

> 费用说明：新账号通常有免费额度。实际使用中，一次完整的试穿+视频流程约花费 3-4 元。

---

## 第五步：配置 API Key

### 方法一：让 Claude 帮你配置（推荐）

直接对 Claude 说：

```
请帮我配置试穿视频技能的 API Key，我的 Key 是：sk-xxxxxxxxxxxx
```

Claude 会自动帮你写入配置文件。

### 方法二：手动配置

1. 用 VS Code 打开文件：`~/.claude/skills/tryon-video-generator/config.json`
   - 如果文件不存在，打开同目录下的 `config.example.json`，另存为 `config.json`
2. 把 `api_key` 后面的内容替换成你的真实 Key
3. 保存文件

配置完成后，文件内容应该类似这样：

```json
{
  "dashscope": {
    "api_key": "sk-你的真实Key",
    "image_model": "wan2.7-image",
    "video_model": "wan2.6-i2v-flash",
    "vl_model": "qwen3-vl-flash"
  },
  "feishu": {
    "app_token": "",
    "table_id": ""
  }
}
```

> `feishu` 部分的 `app_token` 和 `table_id` 留空即可，第一次使用时 Claude 会自动帮你创建飞书表格。

---

## 第六步（可选）：安装飞书上传功能

如果你想把结果自动上传到飞书多维表格，需要安装飞书命令行工具：

1. 确保已安装 Node.js（https://nodejs.org/ ，下载 LTS 版本，双击安装）
2. 在终端中运行：
   ```
   npm install -g @larksuite/cli
   ```
3. 登录飞书：
   ```
   lark-cli auth login --recommend
   ```
4. 按提示在浏览器中完成授权

> 如果你不关心飞书上传，可以跳过这步。效果图和视频会保存在本地，你手动查看就行。

---

## 开始使用

一切就绪！现在你只需要对 Claude 说：

### 触发方式（说任意一句即可）

- `帮我生成试穿视频`
- `试穿视频`
- `虚拟试穿`
- `生成带货视频`

### Claude 会问你拿图片

Claude 会问你：「请提供产品图（服装）和模特图的路径。」

你需要准备：
- **产品图**：一张服装照片（上衣、裙子、裤子等，最好是平铺图或白底图）
- **模特图**：一张人物全身或半身照

把图片的文件路径告诉 Claude，比如：
```
产品图：C:/Users/我的名字/Desktop/dress.jpg
模特图：C:/Users/我的名字/Desktop/model.jpg
```

> 小技巧：可以直接把图片文件拖进 VS Code 终端，会自动显示路径。

### 然后就等着

Claude 会全自动完成以下步骤（约 5-15 分钟）：

1. **分析产品图** — 识别服装类型、颜色、卖点（约 10 秒）
2. **生成多角度提示词** — 为正面/侧面/背面/特写各生成一段融合指令（约 10 秒）
3. **多角度虚拟试穿** — 生成 4 张效果图（约 2-4 分钟）
4. **验证正面图** — 检查效果是否合格（约 10 秒）
5. **生成带货视频** — 10 秒有声营销视频（约 3-10 分钟）
6. **上传飞书** — 自动上传到多维表格（约 30 秒，需完成第六步）

### 查看结果

Claude 最后会告诉你：
- 4 张效果图的保存位置
- 视频文件的保存位置
- 飞书表格的链接（如果配置了飞书上传）

所有文件都在 `~/.claude/skills/tryon-video-generator/workspace/` 目录下。

---

## 常见问题

### Q：提示「未找到 DASHSCOPE_API_KEY」
**A**：API Key 没配置好。回到第五步，确认 `config.json` 文件存在且 `api_key` 不为空。

### Q：图片分析失败
**A**：检查图片格式是否为 JPEG 或 PNG，文件大小是否小于 20MB。微信传图可能被压缩，建议用原图。

### Q：融合效果不好，衣服没有完整穿上去
**A**：这是正常的，AI 有时不完美。Claude 会自动验证正面图并重试一次。如果还是不满意，可以换一张更清晰的产品图（白底、平铺效果最好）。

### Q：视频生成等了很久
**A**：视频生成通常需要 3-10 分钟，请耐心等待。超过 10 分钟会超时，可以再试一次。

### Q：没有飞书表格链接
**A**：需要完成第六步安装 lark-cli。如果不需要飞书上传，可以直接去 workspace 目录查看本地文件。

### Q：一次要花多少钱
**A**：约 3-4 元人民币。其中虚拟试穿 4 张图约 0.8 元，视频生成约 3 元，图片分析约 0.002 元（几乎忽略不计）。

### Q：config.example.json 和 config.json 有什么区别
**A**：`config.example.json` 是模板（不含真实密钥），`config.json` 是你的个人配置（含 API Key）。技能运行时只读取 `config.json`。

---

## 技术参数一览

| 项目 | 说明 |
|------|------|
| 图片分析模型 | qwen3-vl-flash |
| 虚拟试穿模型 | wan2.7-image |
| 视频生成模型 | wan2.6-i2v-flash |
| 视频时长 | 固定 10 秒 |
| 视频分辨率 | 720P |
| 视频音频 | 有声（自动生成语音） |
| 单次费用 | 约 3-4 元 |
